Input: nums = [1,2,2,3,3,3], k = 2
Output: [2,3]

-------------------------------
Things to know
-----------------------------

--> sort the map based upon the values.
--------------------------------------------------
 HashMap<Integer,Integer>mp = new HashMap<>();
-------------------------------------------------------
The map stores:
Key    Value
---------------
1  ->   3
2  ->   2
3  ->   1
-----------------------------------------------------------------------
 List<Map.Entry<Integer,Integer>>list = new ArrayList<>(mp.entrySet());
---------------------------------------------------------------------------
Step 1: mp.entrySet()
returns a Set of Map.Entry objects.
{
   Entry(1,3),
   Entry(2,2),
   Entry(3,1)
}
Each Entry object contains both the key and the value.
---------------------------------------------------
Convert to ArrayList
-----------------------------------------------
List<Map.Entry<Integer,Integer>> list =   new ArrayList<>(mp.entrySet());
Now the ArrayList stores references to these Entry objects.
list
 │
 ├── index 0 ──► Entry
 │                key = 1
 │                value = 3
 │
 ├── index 1 ──► Entry
 │                key = 2
 │                value = 2
 │
 └── index 2 ──► Entry
                  key = 3
                  value = 1
So:
returns the Entry object:
key = 1
value = 3
list.get(0)
HashMap
---------
1 -> 3
2 -> 2
3 -> 1
    │
    ▼
entrySet()

{ Entry(1,3), Entry(2,2), Entry(3,1) }
    │
    ▼
ArrayList

Index      Object Stored

0    ───► Entry(key=1,value=3)

1    ───► Entry(key=2,value=2)

2    ───► Entry(key=3,value=1)
-------------------------------------------------------------
 Collections.sort(list,(a,b)->b.getValue()-a.getValue());
----------------------------------------------------------
What is Collections.sort()?
Collections.sort(list, comparator);
First parameter → list to sort
Second parameter → tells Java how to compare two elements
What are a and b?
During sorting, Java compares two entries at a time.

example:
a = (2,2)
b = (1,3)
a.getKey() = 2
a.getValue() = 2
b.getKey() = 1
b.getValue() = 3
Comparator
(a,b) -> b.getValue() - a.getValue()
means
compare(a,b){
    return b.getValue() - a.getValue();
}
class Solution {
    public int[] topKFrequent(int[] nums, int k) {
        HashMap<Integer,Integer>mp = new HashMap<>();
       
       
        for(int i=0;i<nums.length;i++){
             mp.put(nums[i],mp.getOrDefault(nums[i],0)+1);
            }

            List<Map.Entry<Integer,Integer>>list = new ArrayList<>(mp.entrySet());
            Collections.sort(list,(a,b)->b.getValue()-a.getValue());
          int arr[] = new int[k];
            for(int i=0;i<k;i++){
                arr[i] = list.get(i).getKey();
            }

            return arr;     
    }
}


